// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/takuyakawta/spot-teacher-sample/db/ent/inquiry"
	"github.com/takuyakawta/spot-teacher-sample/db/ent/lessonplan"
	"github.com/takuyakawta/spot-teacher-sample/db/ent/predicate"
	"github.com/takuyakawta/spot-teacher-sample/db/ent/school"
	"github.com/takuyakawta/spot-teacher-sample/db/ent/user"
)

// InquiryQuery is the builder for querying Inquiry entities.
type InquiryQuery struct {
	config
	ctx         *QueryContext
	order       []inquiry.OrderOption
	inters      []Interceptor
	predicates  []predicate.Inquiry
	withLesson  *LessonPlanQuery
	withSchool  *SchoolQuery
	withTeacher *UserQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the InquiryQuery builder.
func (iq *InquiryQuery) Where(ps ...predicate.Inquiry) *InquiryQuery {
	iq.predicates = append(iq.predicates, ps...)
	return iq
}

// Limit the number of records to be returned by this query.
func (iq *InquiryQuery) Limit(limit int) *InquiryQuery {
	iq.ctx.Limit = &limit
	return iq
}

// Offset to start from.
func (iq *InquiryQuery) Offset(offset int) *InquiryQuery {
	iq.ctx.Offset = &offset
	return iq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (iq *InquiryQuery) Unique(unique bool) *InquiryQuery {
	iq.ctx.Unique = &unique
	return iq
}

// Order specifies how the records should be ordered.
func (iq *InquiryQuery) Order(o ...inquiry.OrderOption) *InquiryQuery {
	iq.order = append(iq.order, o...)
	return iq
}

// QueryLesson chains the current query on the "lesson" edge.
func (iq *InquiryQuery) QueryLesson() *LessonPlanQuery {
	query := (&LessonPlanClient{config: iq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := iq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := iq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(inquiry.Table, inquiry.FieldID, selector),
			sqlgraph.To(lessonplan.Table, lessonplan.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, inquiry.LessonTable, inquiry.LessonColumn),
		)
		fromU = sqlgraph.SetNeighbors(iq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QuerySchool chains the current query on the "school" edge.
func (iq *InquiryQuery) QuerySchool() *SchoolQuery {
	query := (&SchoolClient{config: iq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := iq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := iq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(inquiry.Table, inquiry.FieldID, selector),
			sqlgraph.To(school.Table, school.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, inquiry.SchoolTable, inquiry.SchoolColumn),
		)
		fromU = sqlgraph.SetNeighbors(iq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTeacher chains the current query on the "teacher" edge.
func (iq *InquiryQuery) QueryTeacher() *UserQuery {
	query := (&UserClient{config: iq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := iq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := iq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(inquiry.Table, inquiry.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, inquiry.TeacherTable, inquiry.TeacherColumn),
		)
		fromU = sqlgraph.SetNeighbors(iq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Inquiry entity from the query.
// Returns a *NotFoundError when no Inquiry was found.
func (iq *InquiryQuery) First(ctx context.Context) (*Inquiry, error) {
	nodes, err := iq.Limit(1).All(setContextOp(ctx, iq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{inquiry.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (iq *InquiryQuery) FirstX(ctx context.Context) *Inquiry {
	node, err := iq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Inquiry ID from the query.
// Returns a *NotFoundError when no Inquiry ID was found.
func (iq *InquiryQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = iq.Limit(1).IDs(setContextOp(ctx, iq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{inquiry.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (iq *InquiryQuery) FirstIDX(ctx context.Context) int {
	id, err := iq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Inquiry entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Inquiry entity is found.
// Returns a *NotFoundError when no Inquiry entities are found.
func (iq *InquiryQuery) Only(ctx context.Context) (*Inquiry, error) {
	nodes, err := iq.Limit(2).All(setContextOp(ctx, iq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{inquiry.Label}
	default:
		return nil, &NotSingularError{inquiry.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (iq *InquiryQuery) OnlyX(ctx context.Context) *Inquiry {
	node, err := iq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Inquiry ID in the query.
// Returns a *NotSingularError when more than one Inquiry ID is found.
// Returns a *NotFoundError when no entities are found.
func (iq *InquiryQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = iq.Limit(2).IDs(setContextOp(ctx, iq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{inquiry.Label}
	default:
		err = &NotSingularError{inquiry.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (iq *InquiryQuery) OnlyIDX(ctx context.Context) int {
	id, err := iq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Inquiries.
func (iq *InquiryQuery) All(ctx context.Context) ([]*Inquiry, error) {
	ctx = setContextOp(ctx, iq.ctx, ent.OpQueryAll)
	if err := iq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Inquiry, *InquiryQuery]()
	return withInterceptors[[]*Inquiry](ctx, iq, qr, iq.inters)
}

// AllX is like All, but panics if an error occurs.
func (iq *InquiryQuery) AllX(ctx context.Context) []*Inquiry {
	nodes, err := iq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Inquiry IDs.
func (iq *InquiryQuery) IDs(ctx context.Context) (ids []int, err error) {
	if iq.ctx.Unique == nil && iq.path != nil {
		iq.Unique(true)
	}
	ctx = setContextOp(ctx, iq.ctx, ent.OpQueryIDs)
	if err = iq.Select(inquiry.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (iq *InquiryQuery) IDsX(ctx context.Context) []int {
	ids, err := iq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (iq *InquiryQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, iq.ctx, ent.OpQueryCount)
	if err := iq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, iq, querierCount[*InquiryQuery](), iq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (iq *InquiryQuery) CountX(ctx context.Context) int {
	count, err := iq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (iq *InquiryQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, iq.ctx, ent.OpQueryExist)
	switch _, err := iq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (iq *InquiryQuery) ExistX(ctx context.Context) bool {
	exist, err := iq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the InquiryQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (iq *InquiryQuery) Clone() *InquiryQuery {
	if iq == nil {
		return nil
	}
	return &InquiryQuery{
		config:      iq.config,
		ctx:         iq.ctx.Clone(),
		order:       append([]inquiry.OrderOption{}, iq.order...),
		inters:      append([]Interceptor{}, iq.inters...),
		predicates:  append([]predicate.Inquiry{}, iq.predicates...),
		withLesson:  iq.withLesson.Clone(),
		withSchool:  iq.withSchool.Clone(),
		withTeacher: iq.withTeacher.Clone(),
		// clone intermediate query.
		sql:  iq.sql.Clone(),
		path: iq.path,
	}
}

// WithLesson tells the query-builder to eager-load the nodes that are connected to
// the "lesson" edge. The optional arguments are used to configure the query builder of the edge.
func (iq *InquiryQuery) WithLesson(opts ...func(*LessonPlanQuery)) *InquiryQuery {
	query := (&LessonPlanClient{config: iq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	iq.withLesson = query
	return iq
}

// WithSchool tells the query-builder to eager-load the nodes that are connected to
// the "school" edge. The optional arguments are used to configure the query builder of the edge.
func (iq *InquiryQuery) WithSchool(opts ...func(*SchoolQuery)) *InquiryQuery {
	query := (&SchoolClient{config: iq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	iq.withSchool = query
	return iq
}

// WithTeacher tells the query-builder to eager-load the nodes that are connected to
// the "teacher" edge. The optional arguments are used to configure the query builder of the edge.
func (iq *InquiryQuery) WithTeacher(opts ...func(*UserQuery)) *InquiryQuery {
	query := (&UserClient{config: iq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	iq.withTeacher = query
	return iq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Inquiry.Query().
//		GroupBy(inquiry.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (iq *InquiryQuery) GroupBy(field string, fields ...string) *InquiryGroupBy {
	iq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &InquiryGroupBy{build: iq}
	grbuild.flds = &iq.ctx.Fields
	grbuild.label = inquiry.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.Inquiry.Query().
//		Select(inquiry.FieldCreatedAt).
//		Scan(ctx, &v)
func (iq *InquiryQuery) Select(fields ...string) *InquirySelect {
	iq.ctx.Fields = append(iq.ctx.Fields, fields...)
	sbuild := &InquirySelect{InquiryQuery: iq}
	sbuild.label = inquiry.Label
	sbuild.flds, sbuild.scan = &iq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a InquirySelect configured with the given aggregations.
func (iq *InquiryQuery) Aggregate(fns ...AggregateFunc) *InquirySelect {
	return iq.Select().Aggregate(fns...)
}

func (iq *InquiryQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range iq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, iq); err != nil {
				return err
			}
		}
	}
	for _, f := range iq.ctx.Fields {
		if !inquiry.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if iq.path != nil {
		prev, err := iq.path(ctx)
		if err != nil {
			return err
		}
		iq.sql = prev
	}
	return nil
}

func (iq *InquiryQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Inquiry, error) {
	var (
		nodes       = []*Inquiry{}
		_spec       = iq.querySpec()
		loadedTypes = [3]bool{
			iq.withLesson != nil,
			iq.withSchool != nil,
			iq.withTeacher != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Inquiry).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Inquiry{config: iq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, iq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := iq.withLesson; query != nil {
		if err := iq.loadLesson(ctx, query, nodes, nil,
			func(n *Inquiry, e *LessonPlan) { n.Edges.Lesson = e }); err != nil {
			return nil, err
		}
	}
	if query := iq.withSchool; query != nil {
		if err := iq.loadSchool(ctx, query, nodes, nil,
			func(n *Inquiry, e *School) { n.Edges.School = e }); err != nil {
			return nil, err
		}
	}
	if query := iq.withTeacher; query != nil {
		if err := iq.loadTeacher(ctx, query, nodes, nil,
			func(n *Inquiry, e *User) { n.Edges.Teacher = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (iq *InquiryQuery) loadLesson(ctx context.Context, query *LessonPlanQuery, nodes []*Inquiry, init func(*Inquiry), assign func(*Inquiry, *LessonPlan)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Inquiry)
	for i := range nodes {
		fk := nodes[i].LessonScheduleID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(lessonplan.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "lesson_schedule_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (iq *InquiryQuery) loadSchool(ctx context.Context, query *SchoolQuery, nodes []*Inquiry, init func(*Inquiry), assign func(*Inquiry, *School)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Inquiry)
	for i := range nodes {
		fk := nodes[i].SchoolID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(school.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "school_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (iq *InquiryQuery) loadTeacher(ctx context.Context, query *UserQuery, nodes []*Inquiry, init func(*Inquiry), assign func(*Inquiry, *User)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Inquiry)
	for i := range nodes {
		fk := nodes[i].UserID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (iq *InquiryQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := iq.querySpec()
	_spec.Node.Columns = iq.ctx.Fields
	if len(iq.ctx.Fields) > 0 {
		_spec.Unique = iq.ctx.Unique != nil && *iq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, iq.driver, _spec)
}

func (iq *InquiryQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(inquiry.Table, inquiry.Columns, sqlgraph.NewFieldSpec(inquiry.FieldID, field.TypeInt))
	_spec.From = iq.sql
	if unique := iq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if iq.path != nil {
		_spec.Unique = true
	}
	if fields := iq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, inquiry.FieldID)
		for i := range fields {
			if fields[i] != inquiry.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if iq.withLesson != nil {
			_spec.Node.AddColumnOnce(inquiry.FieldLessonScheduleID)
		}
		if iq.withSchool != nil {
			_spec.Node.AddColumnOnce(inquiry.FieldSchoolID)
		}
		if iq.withTeacher != nil {
			_spec.Node.AddColumnOnce(inquiry.FieldUserID)
		}
	}
	if ps := iq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := iq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := iq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := iq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (iq *InquiryQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(iq.driver.Dialect())
	t1 := builder.Table(inquiry.Table)
	columns := iq.ctx.Fields
	if len(columns) == 0 {
		columns = inquiry.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if iq.sql != nil {
		selector = iq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if iq.ctx.Unique != nil && *iq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range iq.predicates {
		p(selector)
	}
	for _, p := range iq.order {
		p(selector)
	}
	if offset := iq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := iq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// InquiryGroupBy is the group-by builder for Inquiry entities.
type InquiryGroupBy struct {
	selector
	build *InquiryQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (igb *InquiryGroupBy) Aggregate(fns ...AggregateFunc) *InquiryGroupBy {
	igb.fns = append(igb.fns, fns...)
	return igb
}

// Scan applies the selector query and scans the result into the given value.
func (igb *InquiryGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, igb.build.ctx, ent.OpQueryGroupBy)
	if err := igb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*InquiryQuery, *InquiryGroupBy](ctx, igb.build, igb, igb.build.inters, v)
}

func (igb *InquiryGroupBy) sqlScan(ctx context.Context, root *InquiryQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(igb.fns))
	for _, fn := range igb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*igb.flds)+len(igb.fns))
		for _, f := range *igb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*igb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := igb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// InquirySelect is the builder for selecting fields of Inquiry entities.
type InquirySelect struct {
	*InquiryQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (is *InquirySelect) Aggregate(fns ...AggregateFunc) *InquirySelect {
	is.fns = append(is.fns, fns...)
	return is
}

// Scan applies the selector query and scans the result into the given value.
func (is *InquirySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, is.ctx, ent.OpQuerySelect)
	if err := is.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*InquiryQuery, *InquirySelect](ctx, is.InquiryQuery, is, is.inters, v)
}

func (is *InquirySelect) sqlScan(ctx context.Context, root *InquiryQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(is.fns))
	for _, fn := range is.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*is.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := is.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
