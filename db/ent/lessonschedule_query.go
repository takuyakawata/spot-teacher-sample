// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/takuyakawta/spot-teacher-sample/db/ent/lessonplan"
	"github.com/takuyakawta/spot-teacher-sample/db/ent/lessonschedule"
	"github.com/takuyakawta/spot-teacher-sample/db/ent/predicate"
)

// LessonScheduleQuery is the builder for querying LessonSchedule entities.
type LessonScheduleQuery struct {
	config
	ctx        *QueryContext
	order      []lessonschedule.OrderOption
	inters     []Interceptor
	predicates []predicate.LessonSchedule
	withPlan   *LessonPlanQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the LessonScheduleQuery builder.
func (lsq *LessonScheduleQuery) Where(ps ...predicate.LessonSchedule) *LessonScheduleQuery {
	lsq.predicates = append(lsq.predicates, ps...)
	return lsq
}

// Limit the number of records to be returned by this query.
func (lsq *LessonScheduleQuery) Limit(limit int) *LessonScheduleQuery {
	lsq.ctx.Limit = &limit
	return lsq
}

// Offset to start from.
func (lsq *LessonScheduleQuery) Offset(offset int) *LessonScheduleQuery {
	lsq.ctx.Offset = &offset
	return lsq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (lsq *LessonScheduleQuery) Unique(unique bool) *LessonScheduleQuery {
	lsq.ctx.Unique = &unique
	return lsq
}

// Order specifies how the records should be ordered.
func (lsq *LessonScheduleQuery) Order(o ...lessonschedule.OrderOption) *LessonScheduleQuery {
	lsq.order = append(lsq.order, o...)
	return lsq
}

// QueryPlan chains the current query on the "plan" edge.
func (lsq *LessonScheduleQuery) QueryPlan() *LessonPlanQuery {
	query := (&LessonPlanClient{config: lsq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := lsq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := lsq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(lessonschedule.Table, lessonschedule.FieldID, selector),
			sqlgraph.To(lessonplan.Table, lessonplan.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, lessonschedule.PlanTable, lessonschedule.PlanColumn),
		)
		fromU = sqlgraph.SetNeighbors(lsq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first LessonSchedule entity from the query.
// Returns a *NotFoundError when no LessonSchedule was found.
func (lsq *LessonScheduleQuery) First(ctx context.Context) (*LessonSchedule, error) {
	nodes, err := lsq.Limit(1).All(setContextOp(ctx, lsq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{lessonschedule.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (lsq *LessonScheduleQuery) FirstX(ctx context.Context) *LessonSchedule {
	node, err := lsq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first LessonSchedule ID from the query.
// Returns a *NotFoundError when no LessonSchedule ID was found.
func (lsq *LessonScheduleQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = lsq.Limit(1).IDs(setContextOp(ctx, lsq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{lessonschedule.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (lsq *LessonScheduleQuery) FirstIDX(ctx context.Context) int64 {
	id, err := lsq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single LessonSchedule entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one LessonSchedule entity is found.
// Returns a *NotFoundError when no LessonSchedule entities are found.
func (lsq *LessonScheduleQuery) Only(ctx context.Context) (*LessonSchedule, error) {
	nodes, err := lsq.Limit(2).All(setContextOp(ctx, lsq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{lessonschedule.Label}
	default:
		return nil, &NotSingularError{lessonschedule.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (lsq *LessonScheduleQuery) OnlyX(ctx context.Context) *LessonSchedule {
	node, err := lsq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only LessonSchedule ID in the query.
// Returns a *NotSingularError when more than one LessonSchedule ID is found.
// Returns a *NotFoundError when no entities are found.
func (lsq *LessonScheduleQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = lsq.Limit(2).IDs(setContextOp(ctx, lsq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{lessonschedule.Label}
	default:
		err = &NotSingularError{lessonschedule.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (lsq *LessonScheduleQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := lsq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of LessonSchedules.
func (lsq *LessonScheduleQuery) All(ctx context.Context) ([]*LessonSchedule, error) {
	ctx = setContextOp(ctx, lsq.ctx, ent.OpQueryAll)
	if err := lsq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*LessonSchedule, *LessonScheduleQuery]()
	return withInterceptors[[]*LessonSchedule](ctx, lsq, qr, lsq.inters)
}

// AllX is like All, but panics if an error occurs.
func (lsq *LessonScheduleQuery) AllX(ctx context.Context) []*LessonSchedule {
	nodes, err := lsq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of LessonSchedule IDs.
func (lsq *LessonScheduleQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if lsq.ctx.Unique == nil && lsq.path != nil {
		lsq.Unique(true)
	}
	ctx = setContextOp(ctx, lsq.ctx, ent.OpQueryIDs)
	if err = lsq.Select(lessonschedule.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (lsq *LessonScheduleQuery) IDsX(ctx context.Context) []int64 {
	ids, err := lsq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (lsq *LessonScheduleQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, lsq.ctx, ent.OpQueryCount)
	if err := lsq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, lsq, querierCount[*LessonScheduleQuery](), lsq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (lsq *LessonScheduleQuery) CountX(ctx context.Context) int {
	count, err := lsq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (lsq *LessonScheduleQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, lsq.ctx, ent.OpQueryExist)
	switch _, err := lsq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (lsq *LessonScheduleQuery) ExistX(ctx context.Context) bool {
	exist, err := lsq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the LessonScheduleQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (lsq *LessonScheduleQuery) Clone() *LessonScheduleQuery {
	if lsq == nil {
		return nil
	}
	return &LessonScheduleQuery{
		config:     lsq.config,
		ctx:        lsq.ctx.Clone(),
		order:      append([]lessonschedule.OrderOption{}, lsq.order...),
		inters:     append([]Interceptor{}, lsq.inters...),
		predicates: append([]predicate.LessonSchedule{}, lsq.predicates...),
		withPlan:   lsq.withPlan.Clone(),
		// clone intermediate query.
		sql:  lsq.sql.Clone(),
		path: lsq.path,
	}
}

// WithPlan tells the query-builder to eager-load the nodes that are connected to
// the "plan" edge. The optional arguments are used to configure the query builder of the edge.
func (lsq *LessonScheduleQuery) WithPlan(opts ...func(*LessonPlanQuery)) *LessonScheduleQuery {
	query := (&LessonPlanClient{config: lsq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	lsq.withPlan = query
	return lsq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		LessonPlanID int64 `json:"lesson_plan_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.LessonSchedule.Query().
//		GroupBy(lessonschedule.FieldLessonPlanID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (lsq *LessonScheduleQuery) GroupBy(field string, fields ...string) *LessonScheduleGroupBy {
	lsq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &LessonScheduleGroupBy{build: lsq}
	grbuild.flds = &lsq.ctx.Fields
	grbuild.label = lessonschedule.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		LessonPlanID int64 `json:"lesson_plan_id,omitempty"`
//	}
//
//	client.LessonSchedule.Query().
//		Select(lessonschedule.FieldLessonPlanID).
//		Scan(ctx, &v)
func (lsq *LessonScheduleQuery) Select(fields ...string) *LessonScheduleSelect {
	lsq.ctx.Fields = append(lsq.ctx.Fields, fields...)
	sbuild := &LessonScheduleSelect{LessonScheduleQuery: lsq}
	sbuild.label = lessonschedule.Label
	sbuild.flds, sbuild.scan = &lsq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a LessonScheduleSelect configured with the given aggregations.
func (lsq *LessonScheduleQuery) Aggregate(fns ...AggregateFunc) *LessonScheduleSelect {
	return lsq.Select().Aggregate(fns...)
}

func (lsq *LessonScheduleQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range lsq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, lsq); err != nil {
				return err
			}
		}
	}
	for _, f := range lsq.ctx.Fields {
		if !lessonschedule.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if lsq.path != nil {
		prev, err := lsq.path(ctx)
		if err != nil {
			return err
		}
		lsq.sql = prev
	}
	return nil
}

func (lsq *LessonScheduleQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*LessonSchedule, error) {
	var (
		nodes       = []*LessonSchedule{}
		_spec       = lsq.querySpec()
		loadedTypes = [1]bool{
			lsq.withPlan != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*LessonSchedule).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &LessonSchedule{config: lsq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, lsq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := lsq.withPlan; query != nil {
		if err := lsq.loadPlan(ctx, query, nodes, nil,
			func(n *LessonSchedule, e *LessonPlan) { n.Edges.Plan = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (lsq *LessonScheduleQuery) loadPlan(ctx context.Context, query *LessonPlanQuery, nodes []*LessonSchedule, init func(*LessonSchedule), assign func(*LessonSchedule, *LessonPlan)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*LessonSchedule)
	for i := range nodes {
		fk := nodes[i].LessonPlanID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(lessonplan.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "lesson_plan_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (lsq *LessonScheduleQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := lsq.querySpec()
	_spec.Node.Columns = lsq.ctx.Fields
	if len(lsq.ctx.Fields) > 0 {
		_spec.Unique = lsq.ctx.Unique != nil && *lsq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, lsq.driver, _spec)
}

func (lsq *LessonScheduleQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(lessonschedule.Table, lessonschedule.Columns, sqlgraph.NewFieldSpec(lessonschedule.FieldID, field.TypeInt64))
	_spec.From = lsq.sql
	if unique := lsq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if lsq.path != nil {
		_spec.Unique = true
	}
	if fields := lsq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, lessonschedule.FieldID)
		for i := range fields {
			if fields[i] != lessonschedule.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if lsq.withPlan != nil {
			_spec.Node.AddColumnOnce(lessonschedule.FieldLessonPlanID)
		}
	}
	if ps := lsq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := lsq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := lsq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := lsq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (lsq *LessonScheduleQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(lsq.driver.Dialect())
	t1 := builder.Table(lessonschedule.Table)
	columns := lsq.ctx.Fields
	if len(columns) == 0 {
		columns = lessonschedule.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if lsq.sql != nil {
		selector = lsq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if lsq.ctx.Unique != nil && *lsq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range lsq.predicates {
		p(selector)
	}
	for _, p := range lsq.order {
		p(selector)
	}
	if offset := lsq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := lsq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// LessonScheduleGroupBy is the group-by builder for LessonSchedule entities.
type LessonScheduleGroupBy struct {
	selector
	build *LessonScheduleQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (lsgb *LessonScheduleGroupBy) Aggregate(fns ...AggregateFunc) *LessonScheduleGroupBy {
	lsgb.fns = append(lsgb.fns, fns...)
	return lsgb
}

// Scan applies the selector query and scans the result into the given value.
func (lsgb *LessonScheduleGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, lsgb.build.ctx, ent.OpQueryGroupBy)
	if err := lsgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LessonScheduleQuery, *LessonScheduleGroupBy](ctx, lsgb.build, lsgb, lsgb.build.inters, v)
}

func (lsgb *LessonScheduleGroupBy) sqlScan(ctx context.Context, root *LessonScheduleQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(lsgb.fns))
	for _, fn := range lsgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*lsgb.flds)+len(lsgb.fns))
		for _, f := range *lsgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*lsgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := lsgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// LessonScheduleSelect is the builder for selecting fields of LessonSchedule entities.
type LessonScheduleSelect struct {
	*LessonScheduleQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (lss *LessonScheduleSelect) Aggregate(fns ...AggregateFunc) *LessonScheduleSelect {
	lss.fns = append(lss.fns, fns...)
	return lss
}

// Scan applies the selector query and scans the result into the given value.
func (lss *LessonScheduleSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, lss.ctx, ent.OpQuerySelect)
	if err := lss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LessonScheduleQuery, *LessonScheduleSelect](ctx, lss.LessonScheduleQuery, lss, lss.inters, v)
}

func (lss *LessonScheduleSelect) sqlScan(ctx context.Context, root *LessonScheduleQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(lss.fns))
	for _, fn := range lss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*lss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := lss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
