// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/takuyakawta/spot-teacher-sample/db/ent/lessonconfirmation"
	"github.com/takuyakawta/spot-teacher-sample/db/ent/lessonreservation"
	"github.com/takuyakawta/spot-teacher-sample/db/ent/predicate"
)

// LessonConfirmationQuery is the builder for querying LessonConfirmation entities.
type LessonConfirmationQuery struct {
	config
	ctx                   *QueryContext
	order                 []lessonconfirmation.OrderOption
	inters                []Interceptor
	predicates            []predicate.LessonConfirmation
	withLessonReservation *LessonReservationQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the LessonConfirmationQuery builder.
func (lcq *LessonConfirmationQuery) Where(ps ...predicate.LessonConfirmation) *LessonConfirmationQuery {
	lcq.predicates = append(lcq.predicates, ps...)
	return lcq
}

// Limit the number of records to be returned by this query.
func (lcq *LessonConfirmationQuery) Limit(limit int) *LessonConfirmationQuery {
	lcq.ctx.Limit = &limit
	return lcq
}

// Offset to start from.
func (lcq *LessonConfirmationQuery) Offset(offset int) *LessonConfirmationQuery {
	lcq.ctx.Offset = &offset
	return lcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (lcq *LessonConfirmationQuery) Unique(unique bool) *LessonConfirmationQuery {
	lcq.ctx.Unique = &unique
	return lcq
}

// Order specifies how the records should be ordered.
func (lcq *LessonConfirmationQuery) Order(o ...lessonconfirmation.OrderOption) *LessonConfirmationQuery {
	lcq.order = append(lcq.order, o...)
	return lcq
}

// QueryLessonReservation chains the current query on the "lesson_reservation" edge.
func (lcq *LessonConfirmationQuery) QueryLessonReservation() *LessonReservationQuery {
	query := (&LessonReservationClient{config: lcq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := lcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := lcq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(lessonconfirmation.Table, lessonconfirmation.FieldID, selector),
			sqlgraph.To(lessonreservation.Table, lessonreservation.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, lessonconfirmation.LessonReservationTable, lessonconfirmation.LessonReservationColumn),
		)
		fromU = sqlgraph.SetNeighbors(lcq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first LessonConfirmation entity from the query.
// Returns a *NotFoundError when no LessonConfirmation was found.
func (lcq *LessonConfirmationQuery) First(ctx context.Context) (*LessonConfirmation, error) {
	nodes, err := lcq.Limit(1).All(setContextOp(ctx, lcq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{lessonconfirmation.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (lcq *LessonConfirmationQuery) FirstX(ctx context.Context) *LessonConfirmation {
	node, err := lcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first LessonConfirmation ID from the query.
// Returns a *NotFoundError when no LessonConfirmation ID was found.
func (lcq *LessonConfirmationQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = lcq.Limit(1).IDs(setContextOp(ctx, lcq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{lessonconfirmation.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (lcq *LessonConfirmationQuery) FirstIDX(ctx context.Context) int64 {
	id, err := lcq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single LessonConfirmation entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one LessonConfirmation entity is found.
// Returns a *NotFoundError when no LessonConfirmation entities are found.
func (lcq *LessonConfirmationQuery) Only(ctx context.Context) (*LessonConfirmation, error) {
	nodes, err := lcq.Limit(2).All(setContextOp(ctx, lcq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{lessonconfirmation.Label}
	default:
		return nil, &NotSingularError{lessonconfirmation.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (lcq *LessonConfirmationQuery) OnlyX(ctx context.Context) *LessonConfirmation {
	node, err := lcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only LessonConfirmation ID in the query.
// Returns a *NotSingularError when more than one LessonConfirmation ID is found.
// Returns a *NotFoundError when no entities are found.
func (lcq *LessonConfirmationQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = lcq.Limit(2).IDs(setContextOp(ctx, lcq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{lessonconfirmation.Label}
	default:
		err = &NotSingularError{lessonconfirmation.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (lcq *LessonConfirmationQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := lcq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of LessonConfirmations.
func (lcq *LessonConfirmationQuery) All(ctx context.Context) ([]*LessonConfirmation, error) {
	ctx = setContextOp(ctx, lcq.ctx, ent.OpQueryAll)
	if err := lcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*LessonConfirmation, *LessonConfirmationQuery]()
	return withInterceptors[[]*LessonConfirmation](ctx, lcq, qr, lcq.inters)
}

// AllX is like All, but panics if an error occurs.
func (lcq *LessonConfirmationQuery) AllX(ctx context.Context) []*LessonConfirmation {
	nodes, err := lcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of LessonConfirmation IDs.
func (lcq *LessonConfirmationQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if lcq.ctx.Unique == nil && lcq.path != nil {
		lcq.Unique(true)
	}
	ctx = setContextOp(ctx, lcq.ctx, ent.OpQueryIDs)
	if err = lcq.Select(lessonconfirmation.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (lcq *LessonConfirmationQuery) IDsX(ctx context.Context) []int64 {
	ids, err := lcq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (lcq *LessonConfirmationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, lcq.ctx, ent.OpQueryCount)
	if err := lcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, lcq, querierCount[*LessonConfirmationQuery](), lcq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (lcq *LessonConfirmationQuery) CountX(ctx context.Context) int {
	count, err := lcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (lcq *LessonConfirmationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, lcq.ctx, ent.OpQueryExist)
	switch _, err := lcq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (lcq *LessonConfirmationQuery) ExistX(ctx context.Context) bool {
	exist, err := lcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the LessonConfirmationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (lcq *LessonConfirmationQuery) Clone() *LessonConfirmationQuery {
	if lcq == nil {
		return nil
	}
	return &LessonConfirmationQuery{
		config:                lcq.config,
		ctx:                   lcq.ctx.Clone(),
		order:                 append([]lessonconfirmation.OrderOption{}, lcq.order...),
		inters:                append([]Interceptor{}, lcq.inters...),
		predicates:            append([]predicate.LessonConfirmation{}, lcq.predicates...),
		withLessonReservation: lcq.withLessonReservation.Clone(),
		// clone intermediate query.
		sql:  lcq.sql.Clone(),
		path: lcq.path,
	}
}

// WithLessonReservation tells the query-builder to eager-load the nodes that are connected to
// the "lesson_reservation" edge. The optional arguments are used to configure the query builder of the edge.
func (lcq *LessonConfirmationQuery) WithLessonReservation(opts ...func(*LessonReservationQuery)) *LessonConfirmationQuery {
	query := (&LessonReservationClient{config: lcq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	lcq.withLessonReservation = query
	return lcq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.LessonConfirmation.Query().
//		GroupBy(lessonconfirmation.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (lcq *LessonConfirmationQuery) GroupBy(field string, fields ...string) *LessonConfirmationGroupBy {
	lcq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &LessonConfirmationGroupBy{build: lcq}
	grbuild.flds = &lcq.ctx.Fields
	grbuild.label = lessonconfirmation.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.LessonConfirmation.Query().
//		Select(lessonconfirmation.FieldCreatedAt).
//		Scan(ctx, &v)
func (lcq *LessonConfirmationQuery) Select(fields ...string) *LessonConfirmationSelect {
	lcq.ctx.Fields = append(lcq.ctx.Fields, fields...)
	sbuild := &LessonConfirmationSelect{LessonConfirmationQuery: lcq}
	sbuild.label = lessonconfirmation.Label
	sbuild.flds, sbuild.scan = &lcq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a LessonConfirmationSelect configured with the given aggregations.
func (lcq *LessonConfirmationQuery) Aggregate(fns ...AggregateFunc) *LessonConfirmationSelect {
	return lcq.Select().Aggregate(fns...)
}

func (lcq *LessonConfirmationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range lcq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, lcq); err != nil {
				return err
			}
		}
	}
	for _, f := range lcq.ctx.Fields {
		if !lessonconfirmation.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if lcq.path != nil {
		prev, err := lcq.path(ctx)
		if err != nil {
			return err
		}
		lcq.sql = prev
	}
	return nil
}

func (lcq *LessonConfirmationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*LessonConfirmation, error) {
	var (
		nodes       = []*LessonConfirmation{}
		_spec       = lcq.querySpec()
		loadedTypes = [1]bool{
			lcq.withLessonReservation != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*LessonConfirmation).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &LessonConfirmation{config: lcq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, lcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := lcq.withLessonReservation; query != nil {
		if err := lcq.loadLessonReservation(ctx, query, nodes, nil,
			func(n *LessonConfirmation, e *LessonReservation) { n.Edges.LessonReservation = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (lcq *LessonConfirmationQuery) loadLessonReservation(ctx context.Context, query *LessonReservationQuery, nodes []*LessonConfirmation, init func(*LessonConfirmation), assign func(*LessonConfirmation, *LessonReservation)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*LessonConfirmation)
	for i := range nodes {
		fk := nodes[i].LessonReservationID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(lessonreservation.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "lesson_reservation_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (lcq *LessonConfirmationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := lcq.querySpec()
	_spec.Node.Columns = lcq.ctx.Fields
	if len(lcq.ctx.Fields) > 0 {
		_spec.Unique = lcq.ctx.Unique != nil && *lcq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, lcq.driver, _spec)
}

func (lcq *LessonConfirmationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(lessonconfirmation.Table, lessonconfirmation.Columns, sqlgraph.NewFieldSpec(lessonconfirmation.FieldID, field.TypeInt64))
	_spec.From = lcq.sql
	if unique := lcq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if lcq.path != nil {
		_spec.Unique = true
	}
	if fields := lcq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, lessonconfirmation.FieldID)
		for i := range fields {
			if fields[i] != lessonconfirmation.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if lcq.withLessonReservation != nil {
			_spec.Node.AddColumnOnce(lessonconfirmation.FieldLessonReservationID)
		}
	}
	if ps := lcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := lcq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := lcq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := lcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (lcq *LessonConfirmationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(lcq.driver.Dialect())
	t1 := builder.Table(lessonconfirmation.Table)
	columns := lcq.ctx.Fields
	if len(columns) == 0 {
		columns = lessonconfirmation.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if lcq.sql != nil {
		selector = lcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if lcq.ctx.Unique != nil && *lcq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range lcq.predicates {
		p(selector)
	}
	for _, p := range lcq.order {
		p(selector)
	}
	if offset := lcq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := lcq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// LessonConfirmationGroupBy is the group-by builder for LessonConfirmation entities.
type LessonConfirmationGroupBy struct {
	selector
	build *LessonConfirmationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (lcgb *LessonConfirmationGroupBy) Aggregate(fns ...AggregateFunc) *LessonConfirmationGroupBy {
	lcgb.fns = append(lcgb.fns, fns...)
	return lcgb
}

// Scan applies the selector query and scans the result into the given value.
func (lcgb *LessonConfirmationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, lcgb.build.ctx, ent.OpQueryGroupBy)
	if err := lcgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LessonConfirmationQuery, *LessonConfirmationGroupBy](ctx, lcgb.build, lcgb, lcgb.build.inters, v)
}

func (lcgb *LessonConfirmationGroupBy) sqlScan(ctx context.Context, root *LessonConfirmationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(lcgb.fns))
	for _, fn := range lcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*lcgb.flds)+len(lcgb.fns))
		for _, f := range *lcgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*lcgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := lcgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// LessonConfirmationSelect is the builder for selecting fields of LessonConfirmation entities.
type LessonConfirmationSelect struct {
	*LessonConfirmationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (lcs *LessonConfirmationSelect) Aggregate(fns ...AggregateFunc) *LessonConfirmationSelect {
	lcs.fns = append(lcs.fns, fns...)
	return lcs
}

// Scan applies the selector query and scans the result into the given value.
func (lcs *LessonConfirmationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, lcs.ctx, ent.OpQuerySelect)
	if err := lcs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LessonConfirmationQuery, *LessonConfirmationSelect](ctx, lcs.LessonConfirmationQuery, lcs, lcs.inters, v)
}

func (lcs *LessonConfirmationSelect) sqlScan(ctx context.Context, root *LessonConfirmationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(lcs.fns))
	for _, fn := range lcs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*lcs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := lcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
